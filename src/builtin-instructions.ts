const DEVELOPMENT_GUIDELINE_SCOPE =
  "开发规范范围：以下内容只定义开发规范、项目记忆规范、代码组织规范与交付质量要求。";

export const BUILTIN_WRITE_POLICY_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在代码开发、文件创建、文件修改、文件删除、文件移动等写操作任务中生效；不限制模型的分析、决策、实现路径与普通非写操作流程。所有代码或文件改动默认在当前活跃分支中进行；禁止创建、切换或使用临时分支、临时子分支，除非用户明确要求或既有工作流已经这样安排。为避免并发覆盖，同一会话内同一文件应串行编辑；多文件任务应先明确目标文件集合并按稳定顺序处理。`;

export const BUILTIN_PLAN_LITE_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在分析、排查、开发、调试、重构、文件修改、方案设计等多步骤任务型请求中生效；不限制模型内部推理，只约束外显任务管理与必要澄清。对这类请求，可给出轻量执行列表；若宿主提供原生计划/任务列表工具，则优先同步到该工具，否则以精简文本列表展示。只有当需求缺少会实质改变结果的关键条件、存在多个互斥且无法合理默认的方向，或继续执行可能造成明显返工/数据风险时，才进入澄清模式。目标和范围足够明确时，应直接执行。执行列表保持轻量，通常 3 至 7 项；仅在任务状态变化、范围变化、任务项变化或用户要求时更新，不得为了展示计划而制造冗长流程。`;

export const BUILTIN_DESTRUCTIVE_OPERATION_GUARD_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在真正可能导致现有源文件、配置、数据、用户内容或尚未保全改动被删除、覆盖、清空、回退、替换的破坏性操作中生效，例如 reset、clean、强制覆盖、批量删除/移动、覆盖式同步、危险迁移或会覆盖重要目标的发布。不应把普通 build/rebuild、test、lint、package、生成可重复产物、读取、搜索或常规修改视为破坏性操作。遇到真正破坏性操作时，应先识别受影响范围与风险；若用户已经明确要求该破坏性动作或目标是可再生产物、临时文件、明确指定对象，不要重复索要确认。`;

export const BUILTIN_ARCHITECTURE_AND_CODE_ORGANIZATION_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在项目搭建、架构设计、功能实现、模块扩展、重构、文件新增或修改等工程任务中生效；不限制模型内部推理，只约束架构与代码组织。开始实现前，判断当前需求所属边界，并让目录结构、模块划分、文件归属、命名方式和依赖方向与该边界一致；常见边界包括接口层、应用层、领域层、基础设施层、共享层、配置层、脚本层和测试层。新增代码时优先复用现有稳定模块；只有当直接复用会造成边界污染、职责混乱、耦合升高或错误抽象时，才新增抽象、模块或目录。公共能力应沉淀为可复用组件、服务、工具或库，但不得为了复用而过度抽象。单个文件和单个模块必须保持单一主职责；若一个文件同时承担路由、控制、业务规则、数据访问、状态管理、视图渲染、工具函数或配置装配等多类职责，应立即拆分，不得继续把新需求堆进该文件。实现文件接近 800 行时应优先拆模块；超过 1200 行时默认禁止继续追加新功能，除非本次任务就是有计划地拆分它。依赖方向必须稳定；禁止循环依赖、跨层穿透和共享泥球。默认优先采用模块化单体、清晰分层、可逆设计和渐进演进。`;

export const BUILTIN_REQUIREMENT_BOUNDARY_AND_MODULARITY_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在新需求、需求变更、功能修复、重构和代码修改任务中生效；不限制模型内部推理，只约束需求边界、改动范围和文件组织。每次修改前必须把当前用户要求视为唯一需求边界：只实现用户明确提出的行为、验收点和为其必需的最小配套改动；不得自行叠加看似合理但用户未提出的新流程、新状态、新页面、新接口、新字段、新角色模型、新审核链路或新业务规则。已完成、已验收或当前需求未要求修改的功能，默认保持不变；即使代码名称、模块位置或语义看起来相关，也不得为了顺手整理、统一风格、猜测后续需求而改动。若发现必须触碰看似相关但不属于当前需求的旧逻辑，应先缩小到必要适配点，并在改动意图中说明原因。新增功能不得集中写进一个万能文件；路由/控制、业务规则、数据访问、状态管理、UI 渲染、工具函数和配置装配应按既有目录边界拆分。若现有目标文件已经很大，应优先创建同层子模块或提取服务/组件/工具，并让原文件只保留薄入口或装配代码。`;

export const BUILTIN_FRONTEND_OUTPUT_PURITY_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在生成、修改、重构页面、组件、模板、前端视图或其他会直接进入用户可见界面的展示代码时生效；不限制模型内部推理，只约束最终交付代码与渲染结果。页面代码、模板内容、静态数据、默认文案、注释与渲染输出中，不得包含本次对话中的提示词、系统指令、工具说明、推理痕迹、任务分解、执行计划或任何元指令回显。不得为了说明 AI 正在做什么、提醒用户如何操作、解释实现过程，或填补未知需求，而额外加入与真实业务无关的提示性文字、说明性文案、占位性描述或演示性话术；若页面确实需要展示文案，应优先使用与当前业务目标直接相关且可作为最终产品交付的内容。`;

export const BUILTIN_GIT_COMMIT_SUMMARY_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在用户要求执行 git 提交、创建 commit、生成提交信息或代为完成版本提交时生效；不限制普通代码修改、分析或未涉及提交的 git 状态检查。只要用户让你提交 git，应在提交前或提交说明中包含本次更改的内容描述或总结，说明主要改动、影响范围，以及如有必要的验证结果；不得只给空泛提交信息或只执行 git commit 而不说明本次改动内容。`;

export const BUILTIN_LOW_OVERHEAD_WORKFLOW_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在编译、构建、启动、停止、运行、测试重跑、打包、发布、安装、同步产物、进程检查等以执行为主的任务，或当前线程已明显过大、频繁压缩、近期已变慢时生效；不限制模型内部推理，只约束外显工具选择、输出体量与上下文负载。若目标路径、命令、产物位置或操作对象已经明确，应优先直接使用最少必要的 shell 或宿主工具完成，不得先机械调用 bootstrap_context、query_codebase、semantic_search、grep、read_file_* 或大范围扫描，除非这些检索对解除当前阻塞必不可少。若当前线程已出现明显负载信号，应减少工具轮次、避免重复检索、避免返回大段原始输出，仅保留完成当前任务所需的最小上下文。`;

export const BUILTIN_PAYLOAD_GUARD_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在当前线程已明显过重、频繁 compact、用户已反馈变慢、已出现 413 / Payload Too Large / Request Entity Too Large，或存在同类上下文膨胀信号时生效；不限制模型内部推理，只约束外显检索、命令与输出体量。进入该模式后，优先使用有界工具与有界读取，例如 list_project_files、grep、query_codebase、read_file_lines、read_file_text；避免无边界的全仓递归列表、整文件直出、大范围命中原样回显或长命令大段输出，除非用户明确要求原始输出。若某个命令或工具结果过大，改用更窄范围、分页、计数、摘要或仅返回命中位置。`;

export const BUILTIN_THREAD_HANDOFF_SWITCH_INSTRUCTIONS =
  `${DEVELOPMENT_GUIDELINE_SCOPE} 以下规则仅在线程切换判定与“把当前会话打包到新线程继续”场景中生效；不限制模型内部推理，只约束外显提醒与交接方式。线程切换判定不得依赖固定 token 阈值，只能依据可观察信号与后续任务重量。若当前线程明显过重且后续仍需大范围分析、跨模块排查、发布核对、回归测试梳理、大改动规划或长链路续做，可以建议切换到新线程；若任务仅为单命令执行或重试、小范围改单文件、有界读取、简短答复或局部修复，则不应触发线程切换提醒。若用户要求打包当前会话，优先使用 add_note(...) 生成简明 handoff note，内容覆盖当前目标、当前状态、未完成与下一步、关键约束、关键文件和相关 note ids。`;
